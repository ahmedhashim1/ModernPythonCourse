from typing import Dict, Union, Optional

Key = Union[int, str]
Value = Union[int, str, list, dict, tuple, set]

data: Dict[Key, Value] = {
    "fname": "Ahmed Hashim",
    "name": "Maheen Ahmed",
    1: "MS-DS"
}
